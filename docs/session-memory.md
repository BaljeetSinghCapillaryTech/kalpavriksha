# Session Memory -- Subscription-CRUD
> Feature: Subscription-CRUD (Subscription Programs Configuration)
> Ticket: aidlc-demo-v2
> Pipeline: feature-pipeline v1.0
> Started: 2026-04-09

## Domain Terminology
| Term | Definition | Source |
|------|-----------|--------|
| Tier | A level in a loyalty program hierarchy (e.g., Silver, Gold, Platinum). Members move between tiers based on KPI thresholds. | BRD S1 |
| Slab | Internal name for a tier in the emf-parent codebase (ProgramSlab entity). | Codebase |
| Benefit | A reward or entitlement attached to a tier -- e.g., points multiplier, free shipping, birthday bonus. BRD treats these as first-class objects, not promotions. | BRD S7.2 |
| Benefit Category | A metadata grouping layer for benefit instances. Categories define type (Welcome Gift, Earn Points, etc.), trigger event, and tier applicability. | BRD Epic 4 |
| Benefit Instance | A configured benefit linked to a specific category and tier, carrying the actual reward value. | BRD Epic 4 |
| KPI | Key Performance Indicator used to evaluate tier eligibility -- e.g., Lifetime Points, Lifetime Spend, Transaction Count. | BRD S7.1 |
| Maker-Checker | Approval workflow where config changes create a pending record that must be approved before going live. | BRD S7.1-US4 |
| aiRa | Capillary's conversational AI assistant, embedded in the configuration UI as a side panel. Handles complex structural changes. | BRD S8 |
| Simulation Mode | Feature allowing users to preview the impact of a config change on current member distribution before publishing. | BRD S7.1-US6 |
| Program | A loyalty program belonging to an org. Contains tiers, benefits, subscriptions. | BRD S1 |
| Subscription | A membership program (tier-based or non-tier) with duration, pricing, benefits, and lifecycle states. | BRD Epic 3 |
| Context Layer | Structured JSON representation of program state that aiRa needs to be intelligent about configuration. | BRD S8.1 |

## Codebase Behaviour
| Behaviour | Evidence | Confidence |
|-----------|----------|------------|
| Extended Fields are entity-type-scoped via ExtendedField.EntityType enum in api/prototype. Current types: CUSTOMER, *_TRANSACTION, *_LINEITEM, LEAD, COMPANY, CARD, USERGROUP2. Each type maps to a MongoDB collection (e.g., customer_extended_fields). No PARTNER_PROGRAM type exists yet. | ExtendedField.java:70-110 in api/prototype | C7 |
| Extended field values stored in MongoDB (ExtendedFieldValidationRuleDaoMongoImpl), configs managed via ExtendedFieldFacade -> ExtendedFieldConfigService. REST API at /v2/entity/extendedFieldConfig (GET/POST). | ExtendedFieldResource.java, ExtendedFieldFacade.java in api/prototype | C7 |
| SubscriptionV2Facade in api/prototype handles COMMUNICATION CHANNEL subscriptions (email/SMS opt-in), NOT partner program subscriptions. These are completely different domains despite sharing the "subscription" name. | SubscriptionV2Facade.java in api/prototype | C7 |
| Partner programs have NO REST APIs in intouch-api-v3. All operations go through Thrift: partnerProgramLinkingEvent, partnerProgramDeLinkingEvent, partnerProgramUpdateEvent in emf.thrift. | emf.thrift, grep of intouch-api-v3 controllers | C6 |
| Maker-checker infrastructure exists in intouch-api-v3 via RequestManagementController (/v3/requests/{entityType}/{entityId}/status) and RequestManagementFacade. EntityType enum has: PROMOTION, TARGET_GROUP, STREAK, LIMIT, LIABILITY_OWNER_SPLIT, WORKFLOW, JOURNEY, BROADCAST_PROMOTION. Currently only PROMOTION is routed. Pattern: add new EntityType value + new domain facade + wire in RequestManagementFacade. | EntityType.java, RequestManagementFacade.java, RequestManagementController.java in intouch-api-v3 | C7 |
| Partner programs only have is_active boolean (no lifecycle state machine). No PartnerProgramStatus enum exists in emf-parent. BRD requires 7 states: Draft, Pending Approval, Scheduled, Active, Paused, Expired, Archived. Resolved by KD-07: lifecycle managed in MongoDB, not MySQL. | PartnerProgram.java in emf-parent, partner_programs.sql in cc-stack-crm | C7 |
| UnifiedPromotion pattern in intouch-api-v3: @Document(collection="unified_promotions") with objectId + unifiedPromotionId (UUID). Metadata nested object holds status (PromotionStatus enum: DRAFT, ACTIVE, PAUSED, PENDING_APPROVAL, STOPPED, SNAPSHOT, LIVE, UPCOMING, COMPLETED, PUBLISH_FAILED), orgId, dates, createdBy. Repository extends MongoRepository with @Query methods for org-scoped + status-scoped lookups. | UnifiedPromotion.java, UnifiedPromotionRepository.java, Metadata.java in intouch-api-v3 | C7 |
| UnifiedPromotion maker-checker: edit ACTIVE creates new DRAFT document (version N+1) with parentId pointing to ACTIVE. Submit for approval -> PENDING_APPROVAL. Approve -> old ACTIVE becomes SNAPSHOT, new doc becomes ACTIVE + publishes to EMF via EntityOrchestrator/PromotionTransformerImpl. Reject -> back to DRAFT. StatusTransitionValidator uses EnumMap<CurrentStatus, Set<PromotionAction>> for valid transitions. | UnifiedPromotionFacade.java:createVersionedPromotion(), changePromotionStatus(), StatusTransitionValidator.java | C7 |
| Multi-tenant MongoDB: EmfMongoConfig configures separate emfMongoTemplate via EmfMongoTenantResolver (per-org database). @EnableMongoRepositories routes UnifiedPromotionRepository to emfMongoTemplate. | EmfMongoConfig.java in intouch-api-v3 | C7 |
| PromotionAction enum: SUBMIT_FOR_APPROVAL, APPROVE, REJECT, PAUSE, STOP, RESUME, REVOKE. Each has getNormalizedAction() for validation consistency. RequestManagementController at /v3/requests/{entityType}/{entityId}/status accepts EntityType path var + PromotionStatus query param + StatusChangeRequest body. | PromotionAction.java, RequestManagementController.java in intouch-api-v3 | C7 |
| **Two Thrift interfaces for partner programs**: (1) `PointsEngineRuleService.Iface` (pointsengine_rules.thrift) has `createOrUpdatePartnerProgram(PartnerProgramInfo, programId, orgId, lastModifiedBy, lastModifiedOn, serverReqId)` for program CONFIG CRUD. `PointsEngineRulesThriftService` in intouch-api-v3 uses this interface -- needs wrapper methods added. (2) `EMFService.Iface` (emf.thrift) has `partnerProgramLinkingEvent/DeLinkingEvent/UpdateEvent` for ENROLLMENT events. `EmfPromotionThriftService` in intouch-api-v3 uses this interface -- needs wrapper methods added. Both connect to "emf-thrift-service" port 9199. | pointsengine_rules.thrift:1269, emf.thrift:1798-1810, PointsEngineRulesThriftService.java, EmfPromotionThriftService.java | C7 _(Phase 2 + user clarification)_ |
| **PartnerProgramInfo struct** (pointsengine_rules.thrift:402-417): partnerProgramId (i32), partnerProgramName (string), description (string), isTierBased (bool), partnerProgramTiers (list, optional), programToPartnerProgramPointsRatio (double), partnerProgramUniqueIdentifier (string, optional), partnerProgramType (EXTERNAL/SUPPLEMENTARY), partnerProgramMembershipCycle (cycleType: DAYS/MONTHS + cycleValue: int, optional), isSyncWithLoyaltyTierOnDowngrade (bool), loyaltySyncTiers (map, optional), updatedViaNewUI (bool, optional), expiryDate (i64, optional), backupProgramId (i32, optional). | pointsengine_rules.thrift:402-417 | C7 _(Phase 2)_ |
| **Subscription ACTIVE transition flow**: (1) Call `PointsEngineRulesThriftService.createOrUpdatePartnerProgram()` to write/update partner_programs MySQL row. (2) Member enrollment is a separate concern via `EmfPromotionThriftService` partner program enrollment methods. The MySQL record MUST exist before enrollment. | User clarification + code verification | C7 _(Phase 2)_ |
| PartnerProgramLinkingEventData requires customerPartnerProgramRef.partnerProgramName (string, required) to identify the program -- uses NAME not ID. The partner_programs MySQL record must pre-exist with a matching name before enrollment can work. | emf.thrift:1297-1303, 1305-1317 | C7 _(Phase 2)_ |
| **Thrift also supports expiry reminders natively**: `createOrUpdateExpiryReminderForPartnerProgram(ExpiryReminderForPartnerProgramInfo, programId, orgId, lastModifiedBy, lastModifiedOn, serverReqId)` and `getAllExpiryReminderConfiguredPartnerPrograms()`. ExpiryReminderForPartnerProgramInfo has: partnerProgramId, partnerProgramName, daysBeforeExpiryReminder, communicationPropertyValues (optional). This maps to KD-09 reminder config. | pointsengine_rules.thrift:419-425, 1276, 1282 | C7 _(Phase 2)_ |
| PartnerProgramUpdateEventData is TIER-SPECIFIC only (upgrade/renew/downgrade/membership_renewal_initiation). It requires PartnerProgramTierUpdateInfo. There is no general-purpose update event for non-tier subscription operations. | emf.thrift:1319-1346 | C7 _(Phase 2)_ |
| RequestManagementController.changeStatus() returns ResponseEntity<ResponseWrapper<UnifiedPromotion>> -- hardcoded return type. SubscriptionFacade returns UnifiedSubscription, requiring either a separate controller method or generalized return type. | RequestManagementController.java in intouch-api-v3 | C7 _(Phase 2)_ |
| StatusChangeRequest DTO has field name "promotionStatus" with @Pattern regex validating only promotion actions. Subscriptions need own DTO or generalized field. | StatusChangeRequest.java in intouch-api-v3 | C7 _(Phase 2)_ |
| EmfMongoConfig has @Profile("!test") -- integration tests need test-specific MongoDB config for SubscriptionRepository. | EmfMongoConfig.java in intouch-api-v3 | C7 _(Phase 2)_ |
| **UnifiedPromotion create flow**: Controller receives @Valid @RequestBody + auth token -> Facade generates UUID (no hyphens) -> validates name uniqueness via ValidatorService -> sets metadata (orgId, createdOn, lastModifiedOn, default DRAFT) -> saves to MongoRepository -> returns doc with generated IDs. | UnifiedPromotionFacade.createUnifiedPromotion():304-400 | C7 _(Phase 5)_ |
| **UnifiedPromotion approve flow (PENDING_APPROVAL -> ACTIVE)**: For new: orchestrator.orchestrate(PUBLISH) -> metadata.setStatus(ACTIVE). For edit-of-active: editOrchestrator.orchestrateEdits() -> old ACTIVE becomes SNAPSHOT, new doc becomes ACTIVE. | UnifiedPromotionFacade.handleNewPromotionApproval():1119, handleEditingScenarioApproval():1009 | C7 _(Phase 5)_ |
| **UnifiedPromotion.getEffectiveStatus()** derives UPCOMING (startDate > now), ENDED (endDate < now), LIVE, PAUSED_FROM_UPCOMING/LIVE/ENDED, DRAFT_FROM_ACTIVE. Pattern for subscription SCHEDULED (KD-11). | UnifiedPromotion.java:151-186 | C7 _(Phase 5)_ |
| **PointsEngineRulesThriftService has NO partner program methods**. Has createOrUpdatePromotionV3, publishPeConfig, getAllPrograms, etc. Needs createOrUpdatePartnerProgram() wrapper method added. | PointsEngineRulesThriftService.java (full read) | C7 _(Phase 5)_ |
| **EmfMongoConfig includeFilters** contains ONLY UnifiedPromotionRepository.class. SubscriptionRepository must be added. | EmfMongoConfig.java:30-33 | C7 _(Phase 5)_ |
| **StatusTransitionValidator states are promotion-specific** (DRAFT, PENDING_APPROVAL, ACTIVE, PAUSED, STOPPED). Subscription has different states (no STOPPED, add EXPIRED/ARCHIVED). Separate validator needed. | StatusTransitionValidator.java:38-39 | C7 _(Phase 5)_ |
| **partner_programs.name is UNIQUE per org**. Must enforce name uniqueness in MongoDB BEFORE Thrift call on APPROVE to avoid MySQL constraint violation. | cc-stack-crm partner_programs.sql:19 | C7 _(Phase 5)_ |
| **Integration test infra**: AbstractContainerTest (Testcontainers: MongoDB, MySQL, Redis, RabbitMQ), EmfMongoConfigTest replaces EmfMongoConfig for test profile, RestTemplate + @LocalServerPort, @MockBean for external services, JUnit 5. | AbstractContainerTest.java, RequestManagementControllerTest.java | C7 _(Phase 5)_ |
| **PromotionAction.getNormalizedAction()** maps RESUME->APPROVE, REVOKE->REJECT. Subscription uses simpler direct mapping (actions are 1:1). | PromotionAction.java:48-59 | C7 _(Phase 5)_ |
| **RequestManagementController returns ResponseEntity<ResponseWrapper<UnifiedPromotion>>** -- hardcoded. Subscription MUST use own controller for status changes. | RequestManagementController.java:39 | C7 _(Phase 5)_ |
| **11 components designed for subscription package**: SubscriptionController, SubscriptionFacade, SubscriptionRepository, SubscriptionStatusTransitionValidator, SubscriptionValidatorService, SubscriptionThriftPublisher, UnifiedSubscription, SubscriptionMetadata, SubscriptionStatus, SubscriptionAction, SubscriptionStatusChangeRequest. Mirrors UnifiedPromotion package structure. | 01-architect.md Section 5 | C7 _(Architect)_ |
| **6-phase implementation plan with dependencies**: Foundation (entity+repo+config) -> CRUD (facade+controller) -> Lifecycle (validator+status changes) -> Thrift Publish (publisher+wire APPROVE) -> Benefits+Approvals (link/unlink/list) -> Maker-Checker Versioning (edit-of-active+approve-with-parent). Phases 4 and 5 can run in parallel. | 01-architect.md Section 12 | C6 _(Architect)_ |
| **Thrift field mapping verified**: 12 fields map from UnifiedSubscription to PartnerProgramInfo. benefitIds, reminders, customFields have NO Thrift equivalent -- live only in MongoDB. partnerProgramId = 0 for create, >0 for update. updatedViaNewUI = true signals v3 origin. | 01-architect.md Section 8.3 | C7 _(Architect)_ |
| **Three Thrift-calling transitions**: APPROVE (create/update with is_active=true), PAUSE (update with is_active=false), RESUME (update with is_active=true). EXPIRED does NOT call Thrift -- MySQL stays is_active=true on expiry. | KD-22, KD-23 | C7 _(User decision)_ |
| **programId is immutable and scopes name uniqueness**: Subscription name must be unique within programId+orgId (not just orgId). programId cannot be changed after creation. Note: MySQL UNIQUE(org_id, name) is broader than MongoDB validation -- a name can repeat across programs in MongoDB but MySQL rejects it. SubscriptionThriftPublisher must handle this edge case. | KD-24 | C7 _(User decision)_ |
| **EmfMongoConfigTest has restricted basePackages**: Test config uses `com.capillary.intouchapiv3.unified.promotion` not the root package. Must expand to include subscription package for ITs. | EmfMongoConfigTest.java:27 | C7 _(Analyst)_ |
| **PointsEngineRulesThriftServiceStub must override new method**: Test stub extends the real service. If createOrUpdatePartnerProgram not overridden, ITs attempt real Thrift connection and fail. | PointsEngineRulesThriftServiceStub.java:26 | C7 _(Analyst)_ |
| **MongoConfig and EmfMongoConfig both scan root package with includeFilters**: If SubscriptionRepository not explicitly routed to EmfMongoConfig, Spring may route it to primary mongoTemplate (wrong DB). | MongoConfig.java:28-36, EmfMongoConfig.java:27-34 | C7 _(Analyst)_ |
| **emfMongoTransactionManager bean exists**: Can be used for atomic ACTIVE<->SNAPSHOT swap in maker-checker. | EmfMongoConfig.java:61-64 | C6 _(Analyst)_ |

## Risks & Concerns
| # | Risk | Severity | Status |
|---|------|----------|--------|
| R-01 | EmfMongoConfig routing failure -- SubscriptionRepository routes to wrong MongoDB template | HIGH | Open _(Analyst)_ |
| R-02 | EmfMongoConfigTest not updated -- integration tests use wrong template silently | HIGH | Open _(Analyst)_ |
| R-03 | PointsEngineRulesThriftServiceStub missing override -- APPROVE ITs fail | HIGH | Open _(Analyst)_ |
| R-04 | MongoDB/MySQL name uniqueness mismatch -- APPROVE returns 500 | MEDIUM | Open _(Analyst)_ |
| R-07 | Concurrent APPROVE race condition -- double Thrift call | MEDIUM | Open _(Analyst)_ |
| R-08 | EXPIRED/MySQL is_active inconsistency -- accepted risk per KD-23 | LOW | Accepted _(Analyst)_ |
| R-10 | Maker-checker SNAPSHOT swap not atomic without transaction | MEDIUM | Open _(Analyst)_ |

## Key Decisions
| # | Decision | Rationale | Phase | Reversible? |
|---|----------|-----------|-------|-------------|
| KD-01 | cc-stack-crm included as a code repo for DB schema reference | Contains DB schema, tables, and indexes needed by all code agents | Phase 0 | Yes |
| KD-02 | **SUPERSEDED** -- Original scope (E1+E4+E2 parts) replaced by KD-03 | Scope correction by user | Phase 0 | N/A |
| KD-03 | Scope: E3 (Subscription Programs) is PRIMARY. Includes: Subscriptions CRUD, maker-checker for subscriptions, subscription-benefits linking with dummy benefit objects. OUT OF SCOPE: E1 (Tier Intelligence), E4 (Benefit Categories -- real implementation deferred, dummy objects used). | User corrected scope -- subscriptions are the focus for this pipeline run | Phase 1 | No -- scope is locked for this run |
| KD-04 | User stories in scope: E3-US2 (Create/Edit CRUD), E3-US4 (Lifecycle Management), E3-US5 (API Contract) + maker-checker. OUT: E3-US1 (Listing UI), E3-US3 (aiRa). Full REST API layer. Benefit linking uses dummy benefit objects. | Backend-focused run; no UI rendering, no AI assistant integration | Phase 1 (BA Q1) | No |
| KD-05 | Tier-based subscriptions use existing codebase model: own slab hierarchy via partner_program_slabs, synced to loyalty tiers via PartnerProgramTierSyncConfiguration. Do NOT implement BRD's direct tier-linking model. | Keep backward compatibility with existing partner program infra; avoid breaking enrolled members | Phase 1 (BA Q2) | No -- structural decision |
| KD-06 | Subscription pricing uses existing Extended Fields mechanism (api/prototype repo), NOT new DB columns. A 'price' named extended field will be created for SUPPLEMENTARY partner programs. Extended fields are stored in MongoDB, defined per entity type via ExtendedField.EntityType enum. Current entity types: CUSTOMER, TRANSACTION, LINEITEM, LEAD, COMPANY, CARD, USERGROUP2. A new PARTNER_PROGRAM entity type will need to be added to support pricing fields on subscriptions. Extended field configs are org-scoped with per-field settings (mandatory, updatable, PII, etc.). | Reuse existing infrastructure; avoid schema changes to partner_programs table; pricing is metadata not core entity data; extended fields already have validation, audit trail, and API support | Phase 1 (BA Q3) | Yes -- could add DB columns later if extended fields prove insufficient |
| KD-07 | **[ADR] MongoDB-first architecture for subscriptions.** All subscription CRUD, lifecycle states (Draft, Pending Approval, Scheduled, Active, Paused, Expired, Archived), maker-checker workflow, and benefit linking stored in MongoDB (intouch-api-v3), following the UnifiedPromotion pattern. MySQL (emf-parent/partner_programs) ONLY updated when subscription transitions to Active, via EMF Thrift APIs. Implementation: (1) New UnifiedSubscription @Document in intouch-api-v3, (2) New SubscriptionRepository extends MongoRepository, (3) Add SUBSCRIPTION to EntityType enum, (4) Extend RequestManagementFacade routing, (5) New SubscriptionFacade, (6) SubscriptionStatusTransitionValidator for subscription-specific states (SCHEDULED, EXPIRED, ARCHIVED not in promotions), (7) On APPROVE -> call EMF Thrift to create/update MySQL partner_programs record, (8) NO schema changes to partner_programs table. Key pattern from UnifiedPromotion: objectId (Mongo auto) + unifiedPromotionId (immutable business UUID); Metadata nested object with status, orgId, dates; edit ACTIVE creates new DRAFT doc (version N+1) with parentId; StatusTransitionValidator uses EnumMap; EmfMongoConfig with EmfMongoTenantResolver for multi-tenant per-org DB routing. | User decision: proven pattern, no MySQL migration risk, flexible MongoDB doc for nested config, maker-checker versioning already works | Phase 1 (BA Q4) | No -- fundamental architecture |
| KD-08 | **[ADR] Benefits are a first-class entity; subscriptions store only foreign key references.** The subscription MongoDB document stores `benefitIds: ["id1", "id2"]` -- a plain array of benefit IDs. No embedded benefit snapshots, no benefit metadata, no separate subscription_benefits collection. Benefits will be built as their own entity in a future pipeline run (E2/E4) with their own CRUD, maker-checker, and audit trail. They will be mappable to Tiers, Subscriptions, and other entities. At read time, benefit details can be resolved by calling the benefits service with the stored IDs. For this run, dummy benefit IDs are used as placeholders -- the API accepts and stores them without validation against a real benefits service. | User decision: clean separation of concerns; benefits are not a subscription sub-entity but a cross-cutting first-class entity; avoids coupling subscription schema to benefit schema; future-proof for E2/E4 pipeline run | Phase 1 (BA Q5) | Yes -- could embed benefit snapshots later if read performance demands it |
| KD-09 | Reminder config and custom field config both stored in the MongoDB subscription document. Reminders: embedded array `reminders: [{daysBefore: N, channel: "SMS/EMAIL/PUSH"}, ...]` (max 5). Custom fields: embedded config `customFields: {meta: [fieldId, ...], link: [fieldId, ...], delink: [fieldId, ...], pause: [fieldId, ...], resume: [fieldId, ...]}` referencing extended field IDs. This run stores CONFIGURATION ONLY -- no reminder triggering logic, no custom field enforcement at enrollment time. Triggering and enforcement are future enhancements. | Config storage is trivial document fields; keeps API contract complete per BRD E3-US5; avoids external system integration (comm-server) in this run; custom field values at enrollment time handled by existing extended fields API | Phase 1 (BA Q6) | Yes -- could remove fields from doc if not needed |
| KD-10 | **[ADR] Enrollment API boundary -- REVISED**: (1) api/prototype v2 linking APIs remain UNCHANGED. (2) intouch-api-v3 v3 subscription CRUD APIs manage config in MongoDB. (3) On ACTIVE transition, v3 calls `PointsEngineRulesThriftService.createOrUpdatePartnerProgram()` to write program config to MySQL. (4) Enrollment (link/delink/renew) stays on EXISTING Thrift paths -- v3 does NOT wrap or modify the enrollment Thrift calls. Only `createOrUpdatePartnerProgram` is called from v3 for program activation. | User revision: enrollment Thrift logic must not be touched. Only program config activation (createOrUpdatePartnerProgram) is called from v3. Enrollment stays on existing paths. | Phase 1 (BA Q7) + Phase 4 (Q5 revision) | No -- defines the service boundary |
| KD-11 | **SCHEDULED is a derived status, not stored.** Store ACTIVE in MongoDB. At read time, if `startDate > now`, return SCHEDULED. Follows UnifiedPromotion's UPCOMING pattern (getEffectiveStatus()). No scheduled job needed for SCHEDULED->ACTIVE transition. State machine has 6 stored states (DRAFT, PENDING_APPROVAL, ACTIVE, PAUSED, EXPIRED, ARCHIVED) + 1 derived (SCHEDULED). | User decision: avoids scheduled job infrastructure. Consistent with proven UnifiedPromotion pattern. | Phase 4 (Blocker Q1) | Yes -- could switch to stored status later if derived logic gets complex |
| KD-12 | **Benefits are shared, reusable entities.** Same benefit ID can be linked to multiple subscriptions AND to other entity types (Tiers, etc.). No uniqueness constraint across subscriptions or entity types. No validation in this run (dummy objects). Cross-entity benefit validation deferred to E2/E4 pipeline run. | User decision: benefits are a cross-cutting shared resource, not scoped to one subscription. | Phase 4 (Blocker Q2) | No -- architectural constraint on benefit model |
| KD-13 | **Maker-checker authorization handled at UI layer.** Backend does NOT enforce approver roles/permissions. Status transition APIs are open to any authenticated caller. Backend trusts the caller. No per-program approver list, no SUBSCRIPTION_APPROVE permission check. | User decision: consistent with how UnifiedPromotion works today. Backend is permission-agnostic for status transitions. | Phase 4 (Blocker Q3) | Yes -- could add permission checks later |
| KD-14 | **PENDING enrollments honored when subscription PAUSED.** Existing PENDING enrollments remain active when subscription transitions to PAUSED. Only new enrollment attempts are blocked (400 response). | User decision: respect commitments made when subscription was ACTIVE. | Phase 4 (Blocker Q4a) | Yes |
| KD-15 | **restrictToOneActivePerMember enforced by EMF Thrift.** v3 API does NOT pre-check this constraint. Delegation to EMF which owns enrollment logic and has ENABLE_PARTNER_PROGRAM_LINKING config. | User decision: avoid duplicating validation. EMF is authoritative. | Phase 4 (Blocker Q4b) | Yes |
| KD-16 | **Enrollment Thrift calls stay on existing paths.** v3 does NOT wrap, modify, or call enrollment Thrift methods (partnerProgramLinkingEvent, partnerProgramDeLinkingEvent, partnerProgramUpdateEvent). Only `createOrUpdatePartnerProgram` is called from v3 on ACTIVE transition. Enrollment operations continue through existing api/prototype v2 paths. This REMOVES Epic 4 (Enrollment Operations) from scope -- E4-US1 through E4-US4 are OUT OF SCOPE for this run. | User decision: do not touch enrollment Thrift logic. Significant scope reduction -- enrollment management stays on existing v2 paths. | Phase 4 (Blocker Q5) | No -- fundamental boundary |
| KD-17 | **[ADR-1] Separate SubscriptionController, NOT RequestManagementFacade routing.** Own controller at `/v3/subscriptions` with dedicated CRUD + status change + benefits endpoints. Does not add SUBSCRIPTION to EntityType or route through RequestManagementFacade. | RequestManagementController hardcodes `ResponseWrapper<UnifiedPromotion>` return type; StatusChangeRequest has promotion-specific field name; subscription is self-contained in its own package. | Phase 6 (Architect) | Yes -- could refactor to shared routing later |
| KD-18 | **[ADR-2] Clone-and-Adapt architectural pattern chosen.** Mirror UnifiedPromotion package structure: entity, repository, facade, controller, status validator, thrift publisher. No shared base classes. No generic entity framework. | Fastest path, lowest risk, proven pattern. Refactoring to shared abstractions deferred until both entities are stable. KD-07 already committed to this direction. | Phase 6 (Architect) | No -- pattern already committed |
| KD-19 | **[ADR-3] EXPIRED is also derived, same as SCHEDULED.** Both derived at read time from ACTIVE status: startDate > now = SCHEDULED, endDate < now = EXPIRED. "List SCHEDULED" = find ACTIVE where startDate > now. "List EXPIRED" = find ACTIVE where endDate < now. | Consistent derivation pattern. Avoids background jobs for expiry transitions. Follows KD-11 principle. | Phase 6 (Architect) | Yes |
| KD-20 | **SubscriptionThriftPublisher maps 12 fields from UnifiedSubscription to PartnerProgramInfo.** Key mappings: metadata.name -> partnerProgramName, unifiedSubscriptionId -> partnerProgramUniqueIdentifier, all v3 subscriptions set partnerProgramType=SUPPLEMENTARY, updatedViaNewUI=true. Fields with no Thrift equivalent (benefitIds, reminders, customFields) live only in MongoDB. | PartnerProgramInfo struct has 14 fields; subscription document is richer. MongoDB is source of truth for full config; MySQL has subset. | Phase 6 (Architect) | No -- Thrift struct is fixed |
| KD-21 | **10 REST API endpoints defined.** CRUD: POST, GET/{id}, GET (list), PUT/{id}, DELETE/{id}. Lifecycle: PUT/{id}/status. Benefits: POST/{id}/benefits, GET/{id}/benefits, DELETE/{id}/benefits. Approvals: GET /approvals. All return ResponseWrapper<UnifiedSubscription>. | Complete API surface per PRD E3-US5. Benefits endpoints accept dummy IDs (KD-08). | Phase 6 (Architect) | Yes -- endpoints can be added/removed |
| KD-22 | **Thrift call on PAUSE and RESUME.** PAUSE calls `createOrUpdatePartnerProgram` with is_active=false. RESUME calls it with is_active=true. MySQL must stay in sync with lifecycle transitions, not just on APPROVE. Three Thrift-calling transitions: APPROVE, PAUSE, RESUME. | User decision: MySQL is_active must reflect actual lifecycle state. EMF enrollment logic checks is_active to determine if new enrollments are allowed. | Phase 6 (OQ-A1) | Yes |
| KD-23 | **EXPIRED does NOT trigger Thrift call.** EXPIRED is derived at read time (endDate < now on ACTIVE). No background job. MySQL stays is_active=true when subscription expires. Inconsistency between MongoDB (shows EXPIRED) and MySQL (is_active=true) is accepted. | User decision: no background jobs in this run. MySQL sync on expiry is a future enhancement. Consistent with KD-11 (no jobs for derived states). | Phase 6 (OQ-A2) | Yes -- could add expiry job later |
| KD-24 | **programId is mandatory and immutable. Name uniqueness scoped to programId, not org.** An org can have multiple loyalty programs. A subscription belongs to exactly ONE program (metadata.programId). programId must be validated at creation time and CANNOT be changed after creation. Subscription name must be unique within a programId (not across the org). Update requests that change programId return 400. | User clarification: programId scopes the subscription to its parent loyalty program. Same name is valid across different programs. MySQL partner_programs UNIQUE(org_id, name) is org-scoped -- MongoDB validation is more granular (per programId). | Phase 6 (OQ-A3) | No -- fundamental scoping |

## Constraints
| # | Constraint | Source | Impact |
|---|-----------|--------|--------|
| C-01 | BRD covers 4 epics: E1 (Tier Intelligence), E2 (Benefits as a Product), E3 (Subscription Programs), E4 (Benefit Categories) -- all P0, Phase 1 | BRD S7 | Scope is large; pipeline may need to focus on a subset per run |
| C-02 | Interface philosophy: Option A (Hybrid -- Direct UI + aiRa) is the recommended starting model | BRD S5 | Architecture must support both direct UI path and aiRa path |
| C-03 | API-First Contract: all APIs must be self-reliant, validation in API not frontend | BRD S10 | Backend must return structured validation errors, not 500s |
| C-04 | 7 open questions in BRD Section 12 still unresolved | BRD S12 | Must resolve during Phase 4 (Blocker Resolution) |
| C-05 | All subscription queries MUST include orgId filter from auth context (tenant isolation). Cross-org access returns 404 not 403. | Architect (ADR-2, G-07) | Every repository query, every facade method |
| C-06 | APPROVE is a blocking Thrift call -- must succeed or APPROVE fails and status rolls back. No async publish. | Architect Section 10.2 | Error handling on Thrift failure must be robust |
| C-07 | Name uniqueness must be validated in MongoDB BEFORE Thrift call on APPROVE to avoid MySQL UNIQUE constraint violation at partner_programs(org_id, name). MongoDB validates per programId (more granular), but MySQL validates per org_id (broader). Must ensure no org-level name collision before Thrift call. | Architect + Phase 5 red flag + KD-24 | SubscriptionValidatorService must check across DRAFT/ACTIVE/PAUSED/PENDING_APPROVAL statuses. Edge case: same name in different programs in MongoDB but rejected by MySQL UNIQUE(org_id, name). |
| C-08 | programId is mandatory at creation, immutable after. Update requests changing programId return 400. | KD-24 | SubscriptionValidatorService + SubscriptionFacade must enforce |

## Open Questions
| # | Question | Source | Status |
|---|----------|--------|--------|
| OQ-01 | Does the program context API exist, or does it need to be built? Latency at p99? | BRD S12 | Open |
| OQ-02 | Impact simulation -- real-time or queued? Acceptable SLA? | BRD S12 | Open |
| OQ-03 | Maker-checker: per-user-role or per-program? Who configures approvers? | BRD S12 | Open |
| OQ-04 | Can a benefit be linked to multiple programs, or always scoped to one? | BRD S12 | Open |
| OQ-05 | Should aiRa handle multi-turn disambiguation? | BRD S12 | Open |
| OQ-06 | Is the downgrade "validate on return transaction" toggle surfaced in new UI or deprecated? | BRD S12 | Open |
| OQ-A1 | Should PAUSE update partner_programs.is_active in MySQL? Currently no Thrift call on PAUSE. | Architect | **RESOLVED** -- Yes, call Thrift on PAUSE (is_active=false) and RESUME (is_active=true). KD-22. |
| OQ-A2 | Should EXPIRED status trigger any Thrift call (e.g., set is_active=false in MySQL)? | Architect | **RESOLVED** -- No. EXPIRED is derived, no Thrift call. MySQL stays is_active=true. KD-23. |
| OQ-A3 | What programId to use for the Thrift call? Always from metadata.programId? | Architect | **RESOLVED** -- Yes, metadata.programId. It is mandatory, immutable, scopes name uniqueness. KD-24. |
