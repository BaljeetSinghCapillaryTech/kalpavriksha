# QA -- Test Scenarios & Edge Cases for Tiers CRUD

> Phase 8: QA
> Date: 2026-04-12
> Source: 00-ba.md, 01-architect.md (ADRs), 03-designer.md, 02-analyst.md (risks), session-memory.md, ui-requirements.md, GUARDRAILS.md
> Confidence: C5 (comprehensive coverage of 52 ACs across 7 user stories, 7 ADRs, 8 risks, 6 guardrail areas)

---

## Acceptance Criteria Coverage Matrix

### US-1: Tier Listing

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US1-AC1 | GET /v3/tiers?programId={id} returns all tiers | TS-L01, TS-L02 | Covered |
| US1-AC2 | Per-tier: basic details (name, description, color, status, duration) | TS-L03 | Covered |
| US1-AC3 | Per-tier: eligibility criteria (type, threshold, AND/OR) | TS-L03 | Covered |
| US1-AC4 | Per-tier: renewal config | TS-L03 | Covered |
| US1-AC5 | Per-tier: downgrade config | TS-L03 | Covered |
| US1-AC6 | Per-tier: linked benefits summary | TS-L04 | Covered |
| US1-AC7 | KPI header: totalTiers, activeTiers, pendingApprovalTiers, totalMembers | TS-L05 | Covered |
| US1-AC8 | Per-tier memberCount cached (5-15 min refresh) | TS-L06, TS-L07 | Covered |
| US1-AC9 | Filterable by status | TS-L08, TS-L09 | Covered |
| US1-AC10 | Tiers ordered by serialNumber (ascending) | TS-L10 | Covered |
| US1-AC11 | Draft/Pending from MongoDB; Active from MongoDB mirror | TS-L11 | Covered |

### US-2: Tier Creation

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US2-AC1 | POST /v3/tiers creates a new tier | TS-C01, TS-C02 | Covered |
| US2-AC2 | Required: name, programId, eligibilityCriteriaType, eligibilityThreshold | TS-C03, TS-C04 | Covered |
| US2-AC3 | Optional: description, color, upgradeSchedule, etc. | TS-C05 | Covered |
| US2-AC4 | Validation: name unique, threshold > previous, serialNumber auto | TS-C06, TS-C07, TS-C08 | Covered |
| US2-AC5 | MC enabled: saves as DRAFT | TS-C01 | Covered |
| US2-AC6 | MC disabled: saves as ACTIVE + SQL sync | TS-C02 | Covered |
| US2-AC7 | serialNumber = max(existing) + 1 | TS-C08 | Covered |
| US2-AC8 | Structured validation errors (400, not 500) | TS-C03, TS-C04 | Covered |
| US2-AC9 | MongoDB doc uses UI field names | TS-C09 | Covered |

### US-3: Tier Editing

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US3-AC1 | PUT /v3/tiers/{tierId} updates a tier | TS-E01 | Covered |
| US3-AC2 | Editing DRAFT: in-place update | TS-E01 | Covered |
| US3-AC3 | Editing ACTIVE: new DRAFT with parentId | TS-E02 | Covered |
| US3-AC4 | Editing PENDING_APPROVAL: in-place update | TS-E03 | Covered |
| US3-AC5 | All editable fields editable | TS-E04 | Covered |
| US3-AC6 | serialNumber NOT editable | TS-E05 | Covered |
| US3-AC7 | Returns validation errors | TS-E06 | Covered |
| US3-AC8 | On approval: new->ACTIVE, old->SNAPSHOT | TS-E07 | Covered |

### US-4: Tier Deletion

> **Rework #2 note**: Deletion scope is now DRAFT-only → DELETED. ACTIVE tier retirement is out of scope (future epic). No MC flow for deletion. No member reassessment on delete. ACs rewritten below.

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US4-AC1 | DELETE /v3/tiers/{tierId} transitions DRAFT tier to DELETED status | TS-D04 | Covered |
| US4-AC2 | ~~Sets status to STOPPED~~ **REMOVED** — STOPPED lifecycle removed | ~~TS-D01~~ | REMOVED |
| US4-AC3 | ~~MC enabled: creates PendingChange for DELETE~~ **REMOVED** — no MC gate for deletion | ~~TS-D02~~ | REMOVED |
| US4-AC4 | 409 if tier is not in DRAFT status | TS-D01, TS-D07 | Covered |
| US4-AC5 | ~~Cannot delete base tier if members assigned~~ **REMOVED** — no member reassessment on DRAFT delete | ~~TS-D03~~ | REMOVED |
| US4-AC6 | ~~Stopped excluded unless includeInactive=true~~ **REMOVED** — STOPPED status removed | ~~TS-D04~~ | REMOVED |
| US4-AC7 | ~~Members flagged for reassessment~~ **REMOVED** — no reassessment on DRAFT-only deletion | ~~TS-D05~~ | REMOVED |

### US-5: Submit for Approval

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US5-AC1 | POST /v3/tiers/{tierId}/submit submits | TS-S01 | Covered |
| US5-AC2 | Generic: TIER, BENEFIT, SUBSCRIPTION (ApprovableEntity types) | TS-S02 | Covered |
| US5-AC3 | Changes status DRAFT -> PENDING_APPROVAL | TS-S01 | Covered |
| US5-AC4 | Records requestedBy, timestamp, payload | TS-S03 | Covered |
| US5-AC5 | Notification hook called | TS-S04 | Covered |

### US-6: Approve/Reject

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US6-AC1 | POST /v3/tiers/{tierId}/approve approves | TS-A01 | Covered |
| US6-AC2 | POST /v3/tiers/{tierId}/reject rejects (comment required) | TS-A02, TS-A03 | Covered |
| US6-AC3 | Approve: calls ApprovableEntityHandler.apply | TS-A04 | Covered |
| US6-AC4 | TierApprovalHandler syncs MongoDB->SQL via Thrift | TS-A05 | Covered |
| US6-AC5 | Reject: PENDING_APPROVAL->DRAFT | TS-A02 | Covered |
| US6-AC6 | Records reviewedBy, timestamp, comment | TS-A06 | Covered |
| US6-AC7 | GET /v3/tiers/approvals lists pending | TS-A07 | Covered |

### US-7: MC Toggle

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| US7-AC1 | isMCEnabled lookup | TS-MC01 | Covered |
| US7-AC2 | Config in org-level settings | TS-MC02 | Covered |
| US7-AC3 | Disabled: Create->ACTIVE, Edit->immediate | TS-MC03 | Covered |
| US7-AC4 | Enabled: Create->DRAFT, Edit->versioned DRAFT | TS-MC04 | Covered |
| US7-AC5 | Toggling doesn't affect existing tiers | TS-MC05 | Covered |

### Phase 7.5 APIs (Get Detail, MC Config, Change Detail)

| AC # | Acceptance Criterion | Test Scenario(s) | Status |
|------|---------------------|-------------------|--------|
| P75-1 | GET /v3/tiers/{tierId} returns full doc + engineConfig | TS-GD01, TS-GD02 | Covered |
| P75-2 | GET /v3/tiers/config returns MC toggle | TS-MC01 | Covered |
| P75-3 | GET /v3/tiers/approvals/{changeId} returns full change + payload | TS-GC01 | Covered |

**Coverage: 52/52 ACs covered. 0 blocked.**

---

## Test Scenarios

### Listing (TS-L*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-L01 | List tiers for program with tiers | GET /v3/tiers?programId=977 | 200 OK, array of tiers with all sections populated |
| TS-L02 | List tiers for program with zero tiers | GET /v3/tiers?programId=99999 | 200 OK, empty tiers array, summary all zeros |
| TS-L03 | Each tier has complete config sections | Check one tier in response | basicDetails, eligibilityCriteria, renewalConfig, downgradeConfig all present and non-null |
| TS-L04 | Benefits linkage returned as benefitIds | Check benefitIds field | Array of benefit ObjectId strings (not full benefit objects) |
| TS-L05 | KPI summary is accurate | Compare summary vs tier array | totalTiers = tiers.length, activeTiers = count(ACTIVE), pendingApprovalTiers = count(PENDING_APPROVAL) |
| TS-L06 | Member counts are cached | Check memberStats field | memberCount is int, lastRefreshed is ISO-8601 timestamp |
| TS-L07 | Stale member count shows last refresh time | memberStats.lastRefreshed is old | Still returns the cached value, not a live query |
| TS-L08 | Status filter works | GET ?status=ACTIVE | Only ACTIVE tiers returned |
| TS-L09 | Multi-status filter | GET ?status=ACTIVE,DRAFT | Both ACTIVE and DRAFT tiers returned, no DELETED |
| TS-L10 | Tiers ordered by serialNumber ASC | Check order | serialNumber 1, 2, 3, ... in order |
| TS-L11 | engineConfig NOT in listing response | Check each tier object | engineConfig field absent or null |
| TS-L12 | Missing programId returns 400 | GET /v3/tiers (no param) | 400 error: "programId is required" |
| TS-L13 | ~~includeInactive=true shows STOPPED~~ | ~~GET ?includeInactive=true~~ | ~~STOPPED tiers included in response~~ | **REMOVED** — STOPPED status no longer exists; DELETED is terminal and never surfaced in listings |
| TS-L14 | ~~includeInactive=false (default) hides STOPPED~~ | ~~GET without includeInactive~~ | ~~STOPPED tiers excluded~~ | **REMOVED** — see TS-L13 note |
| TS-L15 | Max 50 tiers cap | Program with 51 tiers | 400 error or only first 50 returned (per GQ-1) |

### Get Tier Detail (TS-GD*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-GD01 | Get by objectId | GET /v3/tiers/{objectId} | 200 OK with full doc including engineConfig |
| TS-GD02 | Get by unifiedTierId | GET /v3/tiers/{unifiedTierId} | 200 OK, same tier resolved by unifiedTierId |
| TS-GD03 | engineConfig included | Check response | retainPoints, slabUpgradeMode, periodConfig, downgradeEngineConfig, notificationConfig all present |
| TS-GD04 | Non-existent tierId | GET /v3/tiers/nonexistent | 404 error: "Tier not found" |
| TS-GD05 | SNAPSHOT tier accessible | GET /v3/tiers/{snapshotId} | 200 OK, status=SNAPSHOT, read-only |

### Creation (TS-C*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-C01 | Create tier (MC enabled) | POST /v3/tiers with valid body | 201, status=DRAFT, objectId generated, serialNumber auto-assigned |
| TS-C02 | Create tier (MC disabled) | POST /v3/tiers | 201, status=ACTIVE, sqlSlabId populated, SQL slab created |
| TS-C03 | Missing required field: name | POST without basicDetails.name | 400, field-level error: "basicDetails.name is required" |
| TS-C04 | Missing required field: programId | POST without programId | 400, "programId is required" |
| TS-C05 | Create with only required fields | POST with name + programId + eligibility only | 201, optional fields null/default |
| TS-C06 | Duplicate name within program | POST with name "Gold" in program that has Gold | 409 Conflict: "A tier with name 'Gold' already exists" |
| TS-C07 | Duplicate name across programs | POST with name "Gold" in different programId | 201 OK (names unique per program, not globally) |
| TS-C08 | serialNumber auto-increment | Create tier in program with 3 tiers | serialNumber = 4 |
| TS-C09 | Idempotency-Key duplicate | POST same Idempotency-Key twice | Second call returns original response, no duplicate created |
| TS-C10 | Invalid color hex | POST with color "red" | 400, "color must be a valid hex code" |
| TS-C11 | endDate before startDate | POST with endDate < startDate | 400, validation error |
| TS-C12 | [ADR-01] MongoDB doc created on create | Check MongoDB after POST | unified_tier_configs collection has new document |
| TS-C13 | [ADR-01] SQL NOT written on MC-enabled create | Check SQL after POST (MC on) | No new ProgramSlab row (only on approval) |

### Editing (TS-E*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-E01 | Edit DRAFT tier | PUT /v3/tiers/{draftId} | 200, same objectId, updated fields |
| TS-E02 | Edit ACTIVE tier (versioned) | PUT /v3/tiers/{activeId} | 200, NEW objectId, status=DRAFT, parentId=activeId, version=2 |
| TS-E03 | Edit PENDING_APPROVAL tier | PUT /v3/tiers/{pendingId} | 200, same objectId, updated in place |
| TS-E04 | Edit DELETED tier | PUT /v3/tiers/{deletedId} | 400, "Cannot edit a tier in DELETED status" |
| TS-E05 | Cannot change serialNumber | PUT with serialNumber=1 on tier with serialNumber=3 | serialNumber unchanged in response (ignored or 400) |
| TS-E06 | Validation on edit | PUT with invalid eligibilityCriteria | 400, field-level errors |
| TS-E07 | [ADR-04] Approve versioned edit: swap | Approve a DRAFT with parentId | New doc->ACTIVE, old doc->SNAPSHOT |
| TS-E08 | [ADR-04] ACTIVE stays live during pending edit | After creating versioned DRAFT | GET listing shows both: ACTIVE (original) + DRAFT (edit) |
| TS-E09 | One DRAFT per ACTIVE | Edit same ACTIVE twice | Second edit updates existing DRAFT, doesn't create another |
| TS-E10 | Edit preserves engineConfig round-trip | PUT with full body including engineConfig | engineConfig values preserved unchanged in saved doc |

### Deletion (TS-D*)

> **Rework #2 note**: Deletion is DRAFT-only → DELETED. No MC flow. No STOPPED status. TS-D01/D02/D03/D05/D06 are REMOVED. TS-D04 is now the primary happy path. TS-D07/D08 retained with scope adjustment.

| ID | Scenario | Description | Expected Outcome | Rework #2 Status |
|----|----------|-------------|------------------|-----------------|
| TS-D01 | ~~Delete ACTIVE (MC disabled)~~ | ~~DELETE /v3/tiers/{activeId}~~ | ~~204 No Content, status set to STOPPED in MongoDB + SQL~~ | **REMOVED** — ACTIVE deletion out of scope |
| TS-D02 | ~~Delete ACTIVE (MC enabled)~~ | ~~DELETE /v3/tiers/{activeId}~~ | ~~200, PendingChange created with changeType=DELETE~~ | **REMOVED** — no MC flow for deletion |
| TS-D03 | ~~Delete base tier with members~~ | ~~DELETE tier with serialNumber=1, memberCount>0~~ | ~~409, "Cannot stop base tier 'Bronze' -- N members assigned"~~ | **REMOVED** — no member reassessment on DRAFT delete |
| TS-D04 | Delete DRAFT tier | DELETE /v3/tiers/{draftId} | 204 No Content, tier status set to DELETED in MongoDB. No SQL change. No MC gate. | ACTIVE (primary happy path) |
| TS-D05 | ~~Delete triggers reassessment flag~~ | ~~DELETE ACTIVE tier (MC disabled)~~ | ~~Members in that tier flagged for PEB reassessment~~ | **REMOVED** — no member reassessment |
| TS-D06 | ~~Delete tier with PartnerProgramSlabs~~ | ~~DELETE tier referenced by partner slabs~~ | ~~409, "Cannot stop tier -- has active partner program slab mappings"~~ | **REMOVED** — only DRAFT tiers can be deleted; DRAFT has no partner slab refs |
| TS-D07 | Delete non-DRAFT tier returns 409 | DELETE /v3/tiers/{activeId} or DELETE /v3/tiers/{pendingId} | 409, "Tier cannot be deleted. Only DRAFT tiers can be deleted." | UPDATED — covers ACTIVE, PENDING_APPROVAL, SNAPSHOT |
| TS-D08 | Delete non-existent tier | DELETE /v3/tiers/nonexistent | 404 | No change |

### Submit for Approval (TS-S*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-S01 | Submit DRAFT tier | POST /v3/tiers/{draftId}/submit | 200, PendingChange created, tier status->PENDING_APPROVAL |
| TS-S02 | Submit non-TIER entity type | TierFacade.submitForApproval for BENEFIT (generic) | 200 (framework accepts any ApprovableEntity) |
| TS-S03 | PendingChange has full payload | Check PendingChange after submit | payload contains full tier snapshot |
| TS-S04 | NotificationHandler.onSubmit called | Submit a tier | NotificationHandler.onSubmit invoked (no-op default, verify no error) |
| TS-S05 | Submit already ACTIVE tier | POST /v3/tiers/{activeId}/submit | 400, "Tier is in ACTIVE status. Only DRAFT tiers can be submitted." |
| TS-S06 | Submit non-existent entity | POST /v3/tiers/nonexistent/submit | 404, entity not found |
| TS-S07 | Submit already PENDING tier | POST /v3/tiers/{pendingId}/submit | 400, "Tier is already pending approval" |

### Approve/Reject (TS-A*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-A01 | Approve CREATE change | POST /v3/tiers/{tierId}/approve | Tier: PENDING_APPROVAL->ACTIVE, SQL slab created, sqlSlabId populated |
| TS-A02 | Reject with comment | POST /v3/tiers/{tierId}/reject {comment: "Too low"} | Tier: PENDING_APPROVAL->DRAFT, PendingChange: REJECTED |
| TS-A03 | Reject without comment | POST /v3/tiers/{tierId}/reject {} | 400, "Comment is required when rejecting" |
| TS-A04 | [ADR-01] Approve triggers Thrift sync | Approve a tier create | Thrift createSlabAndUpdateStrategies called, SQL ProgramSlab + strategies created |
| TS-A05 | [ADR-07] Atomic Thrift call | Approve a tier | Single Thrift call with SlabInfo + [SLAB_UPGRADE, SLAB_DOWNGRADE] strategies |
| TS-A06 | Approve records reviewer details | Approve a change | PendingChange has reviewedBy, reviewedAt, comment populated |
| TS-A07 | List pending changes | GET /v3/tiers/approvals?programId=977 | Returns all PENDING_APPROVAL changes for tiers |
| TS-A08 | Approve versioned edit (swap) | Approve a DRAFT with parentId | New->ACTIVE, old->SNAPSHOT |
| TS-A09 | ~~Approve DELETE change~~ | ~~Approve a DELETE PendingChange~~ | ~~Tier->STOPPED, SQL status updated~~ | **REMOVED** — no MC flow for deletion; DELETE is immediate on DRAFT tiers |
| TS-A10 | Thrift sync failure rollback | Approve but Thrift call fails | 500, "Failed to sync. Approval rolled back." Tier stays PENDING_APPROVAL |
| TS-A11 | Approve non-existent changeId | POST /v3/tiers/{tierId}/approve (invalid tier) | 404 |
| TS-A12 | Approve already APPROVED change | POST /v3/tiers/{tierId}/approve on an APPROVED change | 400, "Change already processed" |

### Get Change Detail (TS-GC*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-GC01 | Get pending change detail | GET /v3/tiers/approvals/{changeId} | 200 OK with full PendingChange including payload snapshot |
| TS-GC02 | Non-existent changeId | GET /v3/tiers/approvals/nonexistent | 404 |
| TS-GC03 | Payload contains full tier doc | Check payload field | Complete tier snapshot with basicDetails, eligibility, renewal, downgrade, engineConfig |

### MC Toggle (TS-MC*)

| ID | Scenario | Description | Expected Outcome |
|----|----------|-------------|------------------|
| TS-MC01 | Query MC config | GET /v3/tiers/config?programId=977 | 200, makerCheckerEnabled: true/false |
| TS-MC02 | MC config stored in org settings | Check persistence | Config value persists across requests |
| TS-MC03 | MC disabled: create goes ACTIVE | POST /v3/tiers when MC off | 201, status=ACTIVE, SQL synced |
| TS-MC04 | MC enabled: create goes DRAFT | POST /v3/tiers when MC on | 201, status=DRAFT, no SQL sync |
| TS-MC05 | Toggle doesn't affect existing | Toggle MC off with existing DRAFT tier | DRAFT tier stays DRAFT (not auto-activated) |
| TS-MC06 | Missing programId on config query | GET /v3/tiers/config without programId | 400 |
| TS-MC07 | Missing entityType on config query | GET /v3/tiers/config (entityType always TIER) | 400 (not applicable; TIER is implicit) |

---

## Edge Cases & Boundary Conditions

### Data Boundaries

| ID | Edge Case | Expected Behavior |
|----|-----------|-------------------|
| EC-01 | Empty string for tier name | 400 validation error (name required, non-blank) |
| EC-02 | Very long tier name (>255 chars) | 400 validation error (max length) |
| EC-03 | Special characters in tier name (emoji, HTML, SQL injection) | Sanitized/accepted safely. No XSS or SQL injection. [G-03.1, G-03.2] |
| EC-04 | Zero threshold value | Accepted or 400 depending on criteria type (ANY allows null) |
| EC-05 | Negative threshold value | 400 validation error |
| EC-06 | Float threshold value (e.g., 550.5) | Accepted (RM amounts can have decimals) |
| EC-07 | endDate = startDate (zero-duration) | Accepted or 400 (product decision) |
| EC-08 | startDate in the past | Accepted (tier may retroactively start) |
| EC-09 | benefitIds with non-existent IDs | Accepted (lazy validation; benefits may be created later) |
| EC-10 | Empty activities array in eligibilityCriteria | 400, at least one activity required |
| EC-11 | Color hex without # prefix | 400, "color must be a valid hex code" |
| EC-12 | programId = 0 | 400, invalid programId |
| EC-13 | 50 tiers in a program (cap limit) | 51st create returns 400 "Maximum 50 tiers per program" |

### Concurrency & Race Conditions

| ID | Edge Case | Expected Behavior |
|----|-----------|-------------------|
| EC-20 | [G-10] Two users edit same ACTIVE tier simultaneously | First creates DRAFT, second updates same DRAFT (one DRAFT per ACTIVE) |
| EC-21 | [G-10] Two concurrent approvals for same tier | @Lockable prevents concurrent sync. One succeeds, one waits or fails with lock timeout |
| EC-22 | Create two tiers simultaneously in same program | serialNumber correctly assigned without gap or duplicate |
| EC-23 | Delete tier while approval in progress for same tier | 409 or block until approval resolves |
| EC-24 | Edit tier during Thrift sync | @Lockable on TierApprovalHandler prevents MongoDB mutation during sync |

### State Machine Violations

| ID | Edge Case | Expected Behavior |
|----|-----------|-------------------|
| EC-30 | Submit an ACTIVE tier | 400, "Only DRAFT tiers can be submitted" |
| EC-31 | Approve a DRAFT (not submitted) | 400, "Change not found" or "Not in PENDING_APPROVAL status" |
| EC-32 | Reject an APPROVED change | 400, "Change already processed" |
| EC-33 | Edit a SNAPSHOT tier | 400, "Cannot edit a tier in SNAPSHOT status" |
| EC-34 | Delete a SNAPSHOT tier | 400, "Cannot delete terminal-state tier" |
| EC-35 | Submit a DELETED tier | 400, "Cannot submit a DELETED tier" |

### Multi-Tenancy

| ID | Edge Case | Expected Behavior |
|----|-----------|-------------------|
| EC-40 | [G-07.4] Org A creates tier, Org B queries | Org B gets empty result, not Org A's tiers |
| EC-41 | [G-07.4] Org A tries to edit Org B's tier | 404 (tier not found for this org) |
| EC-42 | [G-07.1] Listing without org filter | Server extracts orgId from token; query auto-filtered |
| EC-43 | [G-07.3] Member count cron runs for correct tenant | Cron job carries orgId context; counts are per-org per-program |

### Timezone & Dates

| ID | Edge Case | Expected Behavior |
|----|-----------|-------------------|
| EC-50 | [G-01.7] Create tier with IST timezone date | Server stores as UTC. Response returns ISO-8601 UTC. |
| EC-51 | [G-01.7] Create tier with NPT (+05:45) offset | Converted and stored as UTC correctly |
| EC-52 | [G-01.6] Date without timezone | 400 or auto-assumed UTC (document behavior) |
| EC-53 | [G-01.1] metadata.createdAt uses Instant | Not java.util.Date. Verified as ISO-8601 UTC in response |

---

## ADR-Driven Test Scenarios

| ID | ADR | Test | Expected Outcome |
|----|-----|------|------------------|
| [ADR-01] TS-ADR01 | Dual-Storage | Create tier -> check MongoDB has doc, SQL has NO slab (MC on) | MongoDB doc exists, no ProgramSlab row |
| [ADR-01] TS-ADR02 | Dual-Storage | Approve tier -> check both MongoDB (ACTIVE) and SQL (ProgramSlab created) | Both stores consistent |
| [ADR-02] TS-ADR03 | Generic MC | Submit BENEFIT entity type through same MC framework | Framework accepts, creates PendingChange for BENEFIT |
| [ADR-02] TS-ADR04 | Generic MC | ApprovableEntityHandler dispatches to correct impl per entityType | TIER -> TierApprovalHandler, BENEFIT -> BenefitApprovalHandler (when built) |
| ~~[ADR-03] TS-ADR05~~ | ~~Expand-Then-Contract~~ | ~~Existing findByProgram() still returns ALL slabs (including non-ACTIVE)~~ | ~~No regression on existing engine callers~~ — NOT NEEDED (Rework #3: no SQL changes) |
| ~~[ADR-03] TS-ADR06~~ | ~~Expand-Then-Contract~~ | ~~New findActiveByProgram() excludes non-ACTIVE~~ | ~~Only ACTIVE slabs returned~~ — NOT NEEDED (Rework #3: no findActiveByProgram()) |
| ~~[ADR-03] TS-ADR07~~ | ~~Expand-Then-Contract~~ | ~~Flyway migration sets default 'ACTIVE' for existing rows~~ | ~~All pre-existing program_slabs have status='ACTIVE'~~ — NOT NEEDED (Rework #3: no Flyway migration) |
| [ADR-04] TS-ADR08 | Versioned Edits | Edit ACTIVE -> verify ACTIVE stays live until approved | GET listing: ACTIVE still serves, DRAFT is separate doc |
| [ADR-04] TS-ADR09 | Versioned Edits | Reject versioned edit -> DRAFT reverts, ACTIVE unchanged | ACTIVE version undisturbed |
| [ADR-05] TS-ADR10 | Existing Thrift | TierApprovalHandler calls createSlabAndUpdateStrategies (existing method) | No new Thrift IDL needed; existing Java wrappers used |
| [ADR-06] TS-ADR11 | New Programs Only | Query tiers for old program (no MongoDB docs) | Returns empty from MongoDB tier API (old programs use legacy system) |
| [ADR-07] TS-ADR12 | Atomic Thrift | Approve: strategies updated BEFORE slab created | Execution order: SLAB_UPGRADE strategy -> SLAB_DOWNGRADE strategy -> ProgramSlab -> auto-extend CSV |

---

## Guardrail-Specific Test Scenarios

| ID | Guardrail | Scenario | Expected Outcome |
|----|-----------|----------|------------------|
| GR-01 | G-01.7 | Create tier with dates in UTC, IST (+05:30), NPT (+05:45), US/Eastern | All stored as UTC. Response always ISO-8601 UTC. |
| GR-02 | G-02.3 | POST /v3/tiers with null programId | Fails fast with 400, not NPE in business logic |
| GR-03 | G-02.1 | Tier listing for empty program | Returns empty list [], not null |
| GR-04 | G-02.7 | Unknown TierStatus value in filter | Handled gracefully (400 or ignored), not exception |
| GR-05 | G-03.1 | SQL injection in tier name | Name safely parameterized, no injection |
| GR-06 | G-03.2 | XSS script in description | Sanitized or stored safely, no execution on read |
| GR-07 | G-06.1 | Duplicate POST with same Idempotency-Key | Returns original response, no duplicate |
| GR-08 | G-07.4 | Tenant A data not visible to Tenant B | Cross-tenant query returns empty |
| GR-09 | G-10 | Two concurrent approvals for same tier | @Lockable prevents double-sync. One succeeds, one waits or retries. |
| GR-10 | G-05.4 | Flyway migration backward-compatible | Old app code works with new schema (status column has default) |
| GR-11 | G-06.4 | Correct HTTP status codes | 201 for create, 200 for update, 204 for delete (draft), 400/404/409 for errors |

---

## Risk-Driven Test Scenarios (from 02-analyst.md)

| Risk | Scenario | Expected Outcome |
|------|----------|------------------|
| R1: CSV index off-by-one (HIGH) | Create 4th tier in 3-tier program. Verify threshold CSV has 3 values (positions 0,1,2). | CSV index = serialNumber - 2 for each tier |
| R1: CSV validation | Create 5th tier. Verify all strategy CSVs extended by one position. | allocation_values, expiry_time_values each get one more comma-separated value |
| R2: Downgrade race (MEDIUM) | Approve tier change while PEB downgrade batch is running | @Lockable on TierApprovalHandler serializes access |
| R3: Strategy ID preservation (MEDIUM) | Edit tier eligibility. Verify existing strategy IDs preserved, not recreated. | Strategy update uses existing ID, not insert-new |
| R4: Member count index (MEDIUM) | Run member count cron. Verify it uses INDEX on customer_enrollment(org_id, program_id, current_slab_id, is_active). | Query plan shows index scan, not full table scan |
| R5: Timezone (MEDIUM) | Store date as IST, retrieve, verify UTC | No timezone drift |
| R8: MC self-approval (LOW) | Same user submits AND approves | Product decision: allowed or blocked (test whichever is decided) |

---

## Existing Tests to Extend or Touch

### emf-parent (11 tier + 17 slab test files — regression)
- **SlabUpgradeStrategyImplTest**: Extend with test for new 4th/5th slab threshold CSV position
- **SlabDowngradeStrategyImplTest**: Verify downgrade config skips DELETED-status tiers (not a downgrade target)
- **SlabHelperTest**: Verify serialNumber-to-CSV-index mapping (serialNumber-2)
- **TierDowngradeTest**: Verify downgrade skips DELETED tiers
- **UpgradeSlabActionImplTest**: Verify upgrade logic ignores DELETED slabs

### peb (24 tier test files)
- **TierDowngradeServiceImplTest**: Verify existing findByProgram() still sees all slabs (ADR-03)
- **TierReassessmentServiceImplTest**: ~~Verify reassessment triggered when tier stopped~~ **REMOVED** — no reassessment on DRAFT deletion (Rework #2)
- **PEB*TierDowngradeTest (20+ files)**: Run after Flyway migration to confirm no regression

### intouch-api-v3 (0 existing tier tests -- all new)
- **No existing tier/slab tests found.** All approval and tier CRUD tests are net-new.
- **No existing generic makechecker tests.** Generic MC framework (from Baljeet) is imported; tier-specific handlers are new code.
- Reference: existing UnifiedPromotionController tests for pattern (search for PromotionControllerTest)

---

## Gaps & Coverage Notes

| # | Gap | Impact | Resolution |
|---|-----|--------|------------|
| 1 | No integration test infrastructure for MongoDB + Thrift in intouch-api-v3 | Cannot test full approval flow without Thrift mock | SDET must set up embedded MongoDB + Thrift mock for ITs |
| 2 | Benefits cross-reference in listing (US1-AC6) | benefitIds only; no test for benefit detail fetch | Out of scope (benefits API separate). Test that benefitIds array is returned correctly. |
| 3 | Member count cron job | No test for periodic refresh accuracy | IT should verify: cron runs -> counts updated in MongoDB tier docs |
| 4 | MC notification hook | NoOpNotificationHandler tested for no-error; real notification out of scope | Verify hook is called; verify no-op doesn't throw |
| 5 | secondaryCriteriaEnabled + additionalUpgradeCriteria | BA mentions but no detailed scenarios | Add edge case: secondaryCriteriaEnabled=true with additional criteria config |

---

## Summary

- **Total test scenarios**: 89 (15 listing + 5 get-detail + 13 create + 10 edit + 8 delete + 7 submit + 12 approve/reject + 3 change-detail + 7 MC toggle + 12 ADR + 11 guardrail + risk-driven)
- **Acceptance criteria covered**: 52/52 (100%)
- **ADRs covered**: 7/7
- **Key edge cases**: concurrency (5), state machine violations (6), multi-tenancy (4), timezone (4), data boundaries (13)
- **Existing tests to extend**: ~45 files across emf-parent and peb (regression), 0 in intouch-api-v3 (all new)
- **Most critical gap**: No existing test infrastructure for MongoDB + Thrift integration tests in intouch-api-v3. SDET phase must establish this.

---

# Rework #6a Delta — QA Test Scenarios (Contract Hardening)

> **Phase**: 8 (QA) — cascaded from Phase 7 Designer LLD
> **Date**: 2026-04-22
> **Source**: `03-designer.md` §6a.1–6a.9 (Rework #6a Delta), `01-architect.md` ADR-17R..ADR-21R (as amended)
> **Scope floor**: Contract-hardening only. Zero schema, zero Thrift, zero engine.
> **Intent**: Cover the 8 new reject codes (9011–9018) + the 2 DTO field removals + read-side omit shape. Includes ISTQB suspect-link triage over the 89 pre-existing scenarios above.

---

## 6a.Q1 Suspect-Link Triage over existing 89 scenarios

Rework #6a changes ONLY these upstream items:
- `TierCreateRequest.downgrade` field (REMOVED)
- `TierUpdateRequest.downgrade` field (REMOVED)
- Validator signatures (ADDED `JsonNode rawBody` parameter)
- 8 new reject error codes 9011–9018 (ADDED)
- Read-side wire shape for legacy FIXED tiers missing `periodValue` (CLARIFIED — omit)

Every pre-existing scenario in `04-qa.md` §1–§Summary was triaged against the above. Result:

| Existing TS-ID | Traced Item | Triage | Rationale |
|---|---|---|---|
| **TS-L03** | "downgradeConfig all present and non-null" on LIST response | **CONFIRMED** | Unchanged. Rework #6a removes `downgrade` from the WRITE contract only (ADR-21R write-narrow/read-wide asymmetry). Read path `TierView → TierEnvelope → TierListResponse` continues to render downgrade config from engine state. |
| **TS-C10** | Invalid color hex → 400 | **CONFIRMED** | Validation path unchanged by Rework #6a; color guard pre-dates #6a and isn't in the 9011–9018 band. |
| **TS-C11** | "endDate before startDate → 400" | **UPDATE** | Scenario still valid, but semantics now differ by PeriodType family. For SLAB_UPGRADE-family, this scenario can't fire on write because 9014 (startDate on SLAB_UPGRADE) rejects the request FIRST. Scenario must qualify to FIXED-family only — where `startDate` is still legal. **Edit note appended in §6a.Q4 below**. |
| TS-C01..TS-C09, TS-C12, TS-C13 | create happy-path + MC + idempotency + MongoDB + SQL behaviour | **CONFIRMED** | No overlap with 9011–9018 reject surface; no schema change. |
| TS-E01..TS-E10 | edit / PUT flows | **CONFIRMED** | PUT merge semantics are payload-only (§6a.2.2), but no existing edit TS references the `downgrade` block in the write payload — they reference `renewalConfig`, `eligibility`, etc. |
| TS-D01..TS-D08 | delete | **CONFIRMED** | Unrelated. |
| TS-L01..TS-L15 | listing | **CONFIRMED** (TS-L03 qualified above) | Read-wide contract unchanged. |
| TS-GD01..TS-GD05 | get-detail | **CONFIRMED** | Read-wide contract unchanged. |
| TS-S01..TS-S07 | submit | **CONFIRMED** | Pre-6a. |
| TS-A01..TS-A12 | approve/reject | **CONFIRMED** | Pre-6a. |
| TS-CD01..TS-CD03 | change-detail | **CONFIRMED** | Pre-6a. |
| TS-MC01..TS-MC07 | MC toggle | **CONFIRMED** | Pre-6a. |
| TS-ADR01..TS-ADR12 | ADR-01..ADR-07 compliance | **CONFIRMED** | None in the 8 removed/added surfaces. |
| TS-G01..TS-G11 | guardrail tests | **CONFIRMED** | None touch the 9011–9018 surface. |
| TS-R01..TS-Rn | risk-driven | **CONFIRMED** | R13/R14/R15 are augmented at 6a but their existing scenarios still hold. |

**Total triage**: 1 UPDATE (TS-C11), 0 REGENERATE, 0 OBSOLETE, 88 CONFIRMED, **16 NEW** (§6a.Q2 below).

---

## 6a.Q2 NEW Test Scenarios — Rework #6a reject band (TS-6a-01..TS-6a-16)

Numbering continues the existing TS-* sequence with a `6a-` prefix to make the delta visually distinct and easy for SDET to locate. All scenarios map to concrete error codes defined in `03-designer.md` §6a.4.1.

### Group A — Pre-binding reject scans (Class A, Class B, eligibilityCriteriaType, sentinels)

| TS-ID | Scenario | Steps | Expected | Maps to REQ | Code |
|---|---|---|---|---|---|
| **TS-6a-01** | POST with Class A program-level flag | POST `/v3/tiers` with body containing `"dailyDowngradeEnabled": true` at root OR inside `downgrade`-shaped block | 400 `InvalidInputException` code **9011**; message identifies the rejected field name | REQ-23 | 9011 |
| **TS-6a-02** | POST with second Class A flag variant | POST with `"isDowngradeOnPartnerProgramExpiryEnabled": true` (canonical key per §6a.1 F5) | 400 code **9011** | REQ-23 | 9011 |
| **TS-6a-03** | POST with Class A drift key (PRD wording) | POST with `"isDowngradeOnPartnerProgramDeLinkingEnabled": true` — the PRD-drift name | 400 via Jackson **strict-mode** (unknown-field, generic Jackson 400). NOT 9011 — this key is not on the canonical Class A scanner list. Surfaces test evidence that PRD drift is the wrong key. | R14 regression / C-16 | Jackson 400 |
| **TS-6a-04** | POST with Class B schedule key | POST with `"schedule": [...]` or `"nudges": {...}` at root of tier payload | 400 code **9012** | REQ-24 | 9012 |
| **TS-6a-05** | POST with eligibilityCriteriaType on write | POST with `"eligibility": {"kpiType": "CUMULATIVE_POINTS", "eligibilityCriteriaType": "POINTS"}` | 400 code **9013** | REQ-25 | 9013 |
| **TS-6a-06** | POST with string `"-1"` sentinel in `programId` | POST with `"programId": "-1"` (quoted) | 400 code **9015** | REQ-26 | 9015 |
| **TS-6a-07** | POST with numeric `-1` sentinel in `programId` | POST with `"programId": -1` (unquoted) | 400 code **9016** | REQ-26 | 9016 |
| **TS-6a-08** | POST with legacy `downgrade` block | POST with `"downgrade": {"target": "SINGLE", ...}` at root | 400 via Jackson **strict-mode** (unknown-field path). Generic Jackson 400; NOT code 9011–9018. Asserts Q11 hard-flip behaviour and F1 strict-default. | Q11, ADR-20R | Jackson 400 |

### Group B — Post-binding (typed DTO) rejects

| TS-ID | Scenario | Steps | Expected | Maps to REQ | Code |
|---|---|---|---|---|---|
| **TS-6a-09** | POST with SLAB_UPGRADE + startDate | POST with `"validity": {"periodType": "SLAB_UPGRADE", "startDate": "2026-01-01", "periodValue": null}` | 400 code **9014** | REQ-21, REQ-36 | 9014 |
| **TS-6a-10** | POST with SLAB_UPGRADE_CYCLIC + startDate | POST with `"validity": {"periodType": "SLAB_UPGRADE_CYCLIC", "startDate": "2026-01-01"}` | 400 code **9014** (confirms family-level reject, not single-value) | REQ-21, REQ-36 | 9014 |
| **TS-6a-11** | POST with FIXED, no periodValue | POST with `"validity": {"periodType": "FIXED"}` (periodValue absent) | 400 code **9018** | REQ-56 | 9018 |
| **TS-6a-12** | POST with FIXED_CUSTOMER_REGISTRATION, no periodValue | POST with `"validity": {"periodType": "FIXED_CUSTOMER_REGISTRATION"}` | 400 code **9018** (confirms family-level guard, 2 members) | REQ-56 | 9018 |
| **TS-6a-13** | POST with FIXED, periodValue = 0 | POST with `"validity": {"periodType": "FIXED", "periodValue": 0}` | 400 code **9018** (non-positive guard fires; NOT 9016 because value is 0 not -1) | REQ-56 | 9018 |
| **TS-6a-14** | POST with FIXED, periodValue = -1 (precedence test) | POST with `"validity": {"periodType": "FIXED", "periodValue": -1}` | 400 code **9016** (numeric -1 sentinel fires FIRST; 9018 does not fire because sentinel scan is pre-binding). **Precedence assertion**: 9016 > 9018 for numeric -1 specifically. | REQ-26, REQ-56 | 9016 |
| **TS-6a-15** | POST with FIXED, periodValue = -5 | POST with `"validity": {"periodType": "FIXED", "periodValue": -5}` | 400 code **9018** (sentinel scan only catches -1; -5 flows through to post-binding FIXED-family positive check) | REQ-56 | 9018 |
| **TS-6a-16** | POST with renewal criteriaType lowercase drift | POST with `"validity": {"renewal": {"criteriaType": "same as eligibility"}}` (lowercase) | 400 code **9017** — requires canonical `"Same as eligibility"` literal (`TierRenewalConfig.CRITERIA_SAME_AS_ELIGIBILITY`) | REQ-28, REQ-41 | 9017 |

### Group C — Negative control (must stay 2xx, not blocked by 6a guards)

| TS-ID | Scenario | Steps | Expected | Why it matters |
|---|---|---|---|---|
| **TS-6a-17** | POST SLAB_UPGRADE, no periodValue | POST with `"validity": {"periodType": "SLAB_UPGRADE"}` (no periodValue, no startDate) | **200/201** — valid. periodValue is OPTIONAL for SLAB_UPGRADE family (engine is event-driven). | Prevents REQ-56 from over-broadening and rejecting all SLAB_UPGRADE writes. Critical negative control. |
| **TS-6a-18** | PUT with validity absent | PUT `/v3/tiers/{id}` with `{"name": "Gold Plus"}` (no `validity` key) | **200** — partial update. REQ-56 does NOT fire because payload doesn't touch validity. | Asserts payload-only semantics (§6a.2.2). |
| **TS-6a-19** | PUT transition SLAB_UPGRADE → FIXED, no periodValue in payload | PUT with `"validity": {"periodType": "FIXED"}` only (no periodValue in body; stored tier is SLAB_UPGRADE family) | **400 code 9018** — payload-only validator does NOT merge with stored state; sees FIXED without periodValue and rejects. Asserts §6a.2.2. | Critical: proves post-merge fallback is NOT used. |
| **TS-6a-20** | PUT transition, full valid FIXED payload | PUT with `"validity": {"periodType": "FIXED", "periodValue": 12}` | **200** — valid partial update with full validity block | Positive-path control for PUT transition. |

### Group D — Read-side asymmetric contract (ADR-21R write-narrow / read-wide)

| TS-ID | Scenario | Steps | Expected | Why it matters |
|---|---|---|---|---|
| **TS-6a-21** | GET legacy FIXED tier without `periodValue` | Seed MongoDB with a FIXED tier document created pre-6a that lacks `periodValue`; GET `/v3/tiers/{id}` | 200; response body's `validity.periodValue` field is **OMITTED** (not present as key). NOT `null`, NOT `0`. Assert via JSON key-absence, not value check. | Locks D-6a-2 wire shape. Proves `@JsonInclude(NON_NULL)` path works for the legacy data case. |
| **TS-6a-22** | GET tier with renewal config synthesized | GET tier where engine has no renewal block stored | 200; `validity.renewal` populated with `{criteriaType: "Same as eligibility", ...}` via `TierRenewalNormalizer`. Confirms F4 (normalizer in use). | Read-side symmetry (R4/R5). |
| **TS-6a-23** | GET tier with downgrade config (read-wide) | GET any tier with downgrade strategy in engine | 200; response body includes `downgrade` block (read-wide contract — write-narrow removal from DTO doesn't remove from READ path) | Locks ADR-21R asymmetry. |

### Group E — Cross-cutting and concurrency

| TS-ID | Scenario | Steps | Expected | Why it matters |
|---|---|---|---|---|
| **TS-6a-24** | Unknown-but-unclassified key (Jackson generic) | POST with a truly random key e.g. `"foozle": 42` | 400 via Jackson strict-mode; generic 400 message (not 9011–9018). | Asserts pre-binding scans only catch the 5 classified key families; all other unknown keys fall through to Jackson. |
| **TS-6a-25** | Multi-key request: Class A + sentinel | POST with `"dailyDowngradeEnabled": true, "programId": -1` | 400 code **9011** (first fail-fast — Class A scan runs before sentinel scan) OR code 9016 depending on declared order. **Designer-locked order (§6a.4.4)**: Class A scan FIRST. Expected: **9011**. | Asserts deterministic fail-fast order. |
| **TS-6a-26** | Tenant isolation + reject | POST with valid body to tenant A's program + Class A key | 400 code 9011; the reject must include the correct tenant's error envelope (no cross-tenant leakage of which tenant was queried) | G-07 re-confirm under new reject path. |

---

## 6a.Q3 Acceptance Criteria Coverage — Rework #6a (REQ-21..REQ-56)

| REQ | AC | Scenario(s) | Code |
|---|---|---|---|
| REQ-21 | `validity.startDate` dropped from write contract for SLAB_UPGRADE | TS-6a-09, TS-6a-10 | 9014 |
| REQ-22 | Read-wide tolerance for legacy FIXED tiers missing periodValue | TS-6a-21 | — (read) |
| REQ-23 | Class A program-level flags rejected on per-tier writes | TS-6a-01, TS-6a-02, TS-6a-26 | 9011 |
| REQ-24 | Class B schedule-shaped keys rejected | TS-6a-04 | 9012 |
| REQ-25 | `eligibilityCriteriaType` rejected on write | TS-6a-05 | 9013 |
| REQ-26 | `-1` sentinel rejected (string + numeric) | TS-6a-06, TS-6a-07, TS-6a-14 | 9015, 9016 |
| REQ-27 | Unknown fields hard-reject (Q11 hard-flip) | TS-6a-08, TS-6a-03, TS-6a-24 | Jackson 400 |
| REQ-28 | `renewal.criteriaType` canonical only; drift rejected | TS-6a-16 | 9017 |
| REQ-36 | Same as REQ-21 on PUT | TS-6a-09, TS-6a-10 (apply to PUT) | 9014 |
| REQ-37 | Same as REQ-23 on PUT | TS-6a-01 (PUT variant) | 9011 |
| REQ-38 | Same as REQ-24 on PUT | TS-6a-04 (PUT variant) | 9012 |
| REQ-39 | Same as REQ-25 on PUT | TS-6a-05 (PUT variant) | 9013 |
| REQ-40 | Same as REQ-26 on PUT | TS-6a-06, TS-6a-07 (PUT variants) | 9015, 9016 |
| REQ-41 | Same as REQ-28 on PUT | TS-6a-16 (PUT variant) | 9017 |
| REQ-49 | Unknown fields on PUT hard-reject | TS-6a-08 (PUT variant) | Jackson 400 |
| REQ-56 | FIXED-family requires positive periodValue on both POST + PUT | TS-6a-11, TS-6a-12, TS-6a-13, TS-6a-15, TS-6a-19 | 9018 |

**Coverage**: 16/16 Rework #6a requirements have at least one scenario. 11 requirements have PUT+POST parity coverage (REQ-36..REQ-41, REQ-49, REQ-56). 3 read-wide scenarios (TS-6a-21..23) cover ADR-21R asymmetry.

---

## 6a.Q4 Amendment to TS-C11 (pre-existing scenario — UPDATE triage)

**Existing text** (`04-qa.md` line 159):
> TS-C11 | endDate before startDate | POST with endDate < startDate | 400, validation error

**Amended text** (Rework #6a):
> TS-C11 | endDate before startDate | POST with **`periodType: FIXED` + startDate + endDate < startDate** | 400, validation error. **NOTE**: For SLAB_UPGRADE-family, this scenario cannot fire because TS-6a-09/10 (code 9014) rejects `startDate` pre-binding. TS-C11 is therefore qualified to FIXED-family only (where startDate is still legal on the wire).

This amendment should be applied by the SDET when authoring the test — the text above is the canonical amended scenario.

---

## 6a.Q5 Edge cases and boundary conditions

| Category | Edge Case | Scenario | Notes |
|---|---|---|---|
| Numeric boundaries | `periodValue = Integer.MAX_VALUE` | POST with FIXED + periodValue=2147483647 | 200/201 — positive bound is int32 max; validator accepts |
| Numeric boundaries | `periodValue = Integer.MIN_VALUE` | POST with FIXED + periodValue=-2147483648 | 400 code 9018 (non-positive); 9016 does NOT fire (-1 only) |
| JSON shape | `periodValue` as decimal | POST with FIXED + periodValue=12.5 | 400 via Jackson type mismatch (int-typed field rejects float) — pre-Rework behaviour, unchanged |
| JSON shape | `periodValue` as string "12" | POST with FIXED + periodValue="12" | Depends on Jackson config — default `@JsonFormat` coercion off → Jackson 400. SDET asserts this explicitly. |
| Nested reject | Class A inside validity block | POST with `"validity": {"dailyDowngradeEnabled": true}` | 400 code 9011 — scanner walks ALL levels of the tree, not just root |
| Case sensitivity | `periodType` case drift | POST with `"periodType": "fixed"` (lowercase) | 400 — existing `VALID_PERIOD_TYPES` is case-sensitive; Rework #6a doesn't change this behaviour |
| Whitespace | `criteriaType` trailing space | POST with `"criteriaType": "Same as eligibility "` | 400 code 9017 — canonical check is string-equals (no trim); SDET asserts |
| Array sentinel | `conditions[i].threshold = -1` | POST with nested `-1` in array | 400 code 9016 — scanner must descend into arrays |

---

## 6a.Q6 Compliance coverage — new ADRs

| ADR | Coverage |
|---|---|
| **ADR-17R** (error-code band 9011–9020) | TS-6a-01..16 cover 9011–9018. 9019–9020 remain reserved for future Rework #6b. |
| **ADR-18R** (MC scope preserved) | No new scenarios required — existing TS-S01..TS-A12 scenarios hold. Triaged CONFIRMED above. |
| **ADR-19R** (PeriodType scope + REQ-56) | TS-6a-09..17 + TS-6a-18..20 (PUT parity) + TS-6a-21 (read-side tolerance) |
| **ADR-20R** (Jackson strict) | TS-6a-08, TS-6a-24 (hard reject); TS-6a-03 (drift-key proves strict default) |
| **ADR-21R** (write-narrow / read-wide) | TS-6a-11..15 (write-narrow rejects); TS-6a-21..23 (read-wide preservation) |

---

## 6a.Q7 Forward cascade payload → Phase 8b (Business Test Gen)

**Target phase**: 8b (Business Test Gen — test-case-by-test-case mapping with BT-xx IDs)

**Scope**: 26 new TS-6a-xx scenarios in §6a.Q2 + 1 amendment (TS-C11) + edge cases in §6a.Q5.

**Suspect-link triage instructions for BTG**:
- **CONFIRMED** existing BT-xx that trace to 88 CONFIRMED scenarios above — keep as-is, no regeneration
- **UPDATE** BT-xx that trace to TS-C11 — amend expected-output text; no logic change
- **NEW** BT-xx required for all 26 TS-6a-xx scenarios + 8 edge cases — assign IDs continuing from last BT-xx in `04b-business-tests.md`
- Each new BT-xx must trace to: (a) one TS-6a-xx, (b) exactly one REQ-xx (see §6a.Q3), (c) one Designer interface method from §6a.4.3, (d) zero or more ADRs from §6a.Q6

**Open contradictions remaining at QA**: NONE. All 5 resolved at Designer (§6a.5 in `03-designer.md`).

**Scope floor**: Contract-hardening only — zero schema, zero Thrift, zero engine.

---

## 6a.Q8 Summary (post-Rework #6a)

- **Net new scenarios**: 26 (16 primary + 3 PUT-parity + 3 read-side + 3 concurrency/cross-cutting + 1 amendment)
- **Existing scenarios amended**: 1 (TS-C11)
- **Existing scenarios untouched**: 88
- **Total post-6a**: 89 + 26 = **115 test scenarios**
- **Acceptance criteria**: 52 pre-6a + 16 Rework #6a = **68 ACs covered at 100%**
- **Error codes covered**: 9001–9010 (pre-6a) + 9011–9018 (Rework #6a) = **18 codes**
- **ISTQB triage summary**: 0 OBSOLETE, 0 REGENERATE, 1 UPDATE, 88 CONFIRMED, 26 NEW
